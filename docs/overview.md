# Evaluation overview

This folder can host longer-form descriptions of the decision scenarios, evaluation rubric, or data provenance. 

## Experiment notes
- Record dataset sources under `data/` and reference the corresponding scenario IDs.
- Track which models and hyperparameters produced a given output in `experiments/<timestamp>/`.
